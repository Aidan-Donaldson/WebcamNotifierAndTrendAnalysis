To get this to work you will need to have an email to use for the sender address and its app password.
You will also need to create a sqlite database.
Find the comments in Manager.py and replace those fields with the corresponding data.
